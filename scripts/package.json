{
  "name": "@workspace/scripts",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "audit:evidence-migration": "tsx ./src/audit-evidence-migration.ts",
    "benchmark:platform": "tsx ./src/benchmark-platform.ts",
    "hello": "tsx ./src/hello.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@workspace/db": "workspace:*",
    "@types/node": "catalog:",
    "tsx": "catalog:"
  }
}
