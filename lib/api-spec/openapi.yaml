openapi: 3.1.0
info:
  # Do not change the title, if the title changes, the import paths will be broken
  title: Api
  version: 0.1.0
  description: N-Tech C³ Engineering Intelligence Operating System API
servers:
  - url: /api
    description: Base API path
tags:
  - name: health
    description: Health operations
  - name: stories
    description: Story operations
  - name: campaigns
    description: Campaign operations
  - name: evidence
    description: Evidence vault operations
  - name: assets
    description: Asset library operations
  - name: knowledge
    description: Knowledge base operations
  - name: templates
    description: Template operations
  - name: projects
    description: Deprecated Project compatibility operations
  - name: workspaces
    description: Workspace operating-context operations
  - name: dashboard
    description: Dashboard and analytics operations
  - name: search
    description: Cross-module full-text search

paths:
  /healthz:
    get:
      operationId: healthCheck
      tags: [health]
      summary: Health check
      responses:
        "200":
          description: Healthy
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HealthStatus"

  # Dashboard
  /dashboard/stats:
    get:
      operationId: getDashboardStats
      tags: [dashboard]
      summary: Get dashboard statistics
      responses:
        "200":
          description: Dashboard statistics
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DashboardStats"

  /activity:
    get:
      operationId: getRecentActivity
      tags: [dashboard]
      summary: Get recent activity feed
      responses:
        "200":
          description: Recent activity items
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/ActivityItem"

  /search:
    get:
      operationId: globalSearch
      tags: [search]
      summary: Search all indexed engineering intelligence
      parameters:
        - name: q
          in: query
          required: true
          schema:
            type: string
            minLength: 2
        - name: limit
          in: query
          schema:
            type: number
            minimum: 1
            maximum: 50
            default: 20
        - name: entityType
          in: query
          schema:
            type: string
            enum: [story, evidence, knowledge, campaign, asset, template, workspace]
        - name: projectId
          in: query
          schema:
            type: number
        - name: status
          in: query
          schema:
            type: string
        - name: from
          in: query
          schema:
            type: string
            format: date
        - name: to
          in: query
          schema:
            type: string
            format: date
      responses:
        "200":
          description: Ranked search results
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/SearchResult"

  # Workspaces
  /workspaces:
    get:
      operationId: listWorkspaces
      tags: [workspaces]
      summary: List and filter workspaces
      parameters:
        - name: search
          in: query
          schema:
            type: string
        - name: status
          in: query
          schema:
            type: string
            enum: [Active, Archived, Corrupted]
        - name: favorite
          in: query
          schema:
            type: boolean
        - name: pinned
          in: query
          schema:
            type: boolean
        - name: purpose
          in: query
          schema:
            type: string
      responses:
        "200":
          description: Workspace summaries
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/WorkspaceSummary"
    post:
      operationId: createWorkspace
      tags: [workspaces]
      summary: Create a workspace
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/WorkspaceInput"
      responses:
        "201":
          description: Created workspace
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Workspace"
        "409":
          description: Workspace slug already exists

  /workspaces/{workspaceId}:
    parameters:
      - name: workspaceId
        in: path
        required: true
        schema:
          type: number
    get:
      operationId: getWorkspace
      tags: [workspaces]
      summary: Get a workspace overview
      responses:
        "200":
          description: Workspace overview
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WorkspaceOverview"
        "404":
          description: Not found
    patch:
      operationId: updateWorkspace
      tags: [workspaces]
      summary: Update workspace metadata or state
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/WorkspacePatch"
      responses:
        "200":
          description: Updated workspace
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Workspace"
        "404":
          description: Not found
        "409":
          description: Archived workspace is read-only

  /workspaces/{workspaceId}/duplicate:
    post:
      operationId: duplicateWorkspace
      tags: [workspaces]
      summary: Duplicate workspace metadata and DNA
      parameters:
        - name: workspaceId
          in: path
          required: true
          schema:
            type: number
      responses:
        "201":
          description: Duplicated workspace
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Workspace"
        "404":
          description: Not found

  /workspaces/{workspaceId}/integrity:
    get:
      operationId: getWorkspaceIntegrity
      tags: [workspaces]
      summary: Check workspace referential integrity
      parameters:
        - name: workspaceId
          in: path
          required: true
          schema:
            type: number
      responses:
        "200":
          description: Integrity report
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WorkspaceIntegrity"
        "404":
          description: Not found

  /workspaces/{workspaceId}/export:
    get:
      operationId: exportWorkspace
      tags: [workspaces]
      summary: Export a versioned workspace manifest
      parameters:
        - name: workspaceId
          in: path
          required: true
          schema:
            type: number
      responses:
        "200":
          description: Workspace manifest
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WorkspaceManifest"
        "404":
          description: Not found

  # Deprecated Projects compatibility API
  /projects:
    get:
      operationId: listProjects
      tags: [projects]
      summary: List all projects
      responses:
        "200":
          description: List of projects
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Project"
    post:
      operationId: createProject
      tags: [projects]
      summary: Create a project
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ProjectInput"
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Project"

  /projects/{id}:
    get:
      operationId: getProject
      tags: [projects]
      summary: Get a project
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: number
      responses:
        "200":
          description: Project
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Project"
        "404":
          description: Not found
    patch:
      operationId: updateProject
      tags: [projects]
      summary: Update a project
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: number
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ProjectPatch"
      responses:
        "200":
          description: Updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Project"
        "404":
          description: Not found
    delete:
      operationId: deleteProject
      tags: [projects]
      summary: Delete a project
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: number
      responses:
        "204":
          description: Deleted
        "404":
          description: Not found

  # Stories
  /stories:
    get:
      operationId: listStories
      tags: [stories]
      summary: List stories
      parameters:
        - name: status
          in: query
          schema:
            type: string
        - name: campaignId
          in: query
          schema:
            type: ["number", "null"]
        - name: workspaceId
          in: query
          schema:
            type: number
        - name: storyType
          in: query
          schema:
            type: string
        - name: search
          in: query
          schema:
            type: string
      responses:
        "200":
          description: List of stories
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Story"
    post:
      operationId: createStory
      tags: [stories]
      summary: Create a story
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/StoryInput"
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Story"

  /stories/{id}:
    get:
      operationId: getStory
      tags: [stories]
      summary: Get a story
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: number
      responses:
        "200":
          description: Story
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Story"
        "404":
          description: Not found
    patch:
      operationId: updateStory
      tags: [stories]
      summary: Update a story
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: number
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/StoryPatch"
      responses:
        "200":
          description: Updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Story"
        "404":
          description: Not found
    delete:
      operationId: deleteStory
      tags: [stories]
      summary: Delete a story
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: number
      responses:
        "204":
          description: Deleted
        "404":
          description: Not found

  /stories/by-status:
    get:
      operationId: getStoriesByStatus
      tags: [stories]
      summary: Get story counts grouped by status
      responses:
        "200":
          description: Story counts by status
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/StatusCount"

  /stories/{id}/transition:
    post:
      operationId: transitionStory
      tags: [stories]
      summary: Validate and apply a Story lifecycle transition
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: number
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/StoryTransitionInput"
      responses:
        "200":
          description: Transitioned Story
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Story"
        "409":
          description: Transition blocked

  /stories/{id}/outline:
    get:
      operationId: getStoryOutline
      tags: [stories]
      summary: Get ordered Story outline
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: number
      responses:
        "200":
          description: Story outline
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/StoryOutlineItem"
    put:
      operationId: replaceStoryOutline
      tags: [stories]
      summary: Atomically replace ordered Story outline
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: number
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: array
              items:
                $ref: "#/components/schemas/StoryOutlineItemInput"
      responses:
        "200":
          description: Updated outline
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/StoryOutlineItem"

  /stories/{id}/links:
    get:
      operationId: getStoryLinks
      tags: [stories]
      summary: Get Story Evidence, Knowledge, Asset, Campaign, and related Story links
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: number
      responses:
        "200":
          description: Story links
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/StoryLinks"
    post:
      operationId: linkStoryEntity
      tags: [stories]
      summary: Link an entity to a Story
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: number
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/StoryLinkInput"
      responses:
        "201":
          description: Link created
  /stories/{id}/links/{entityType}/{entityId}:
    delete:
      operationId: unlinkStoryEntity
      tags: [stories]
      summary: Unlink an entity from a Story
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: number
        - name: entityType
          in: path
          required: true
          schema:
            type: string
            enum: [evidence, knowledge, asset, campaign, story]
        - name: entityId
          in: path
          required: true
          schema:
            type: number
      responses:
        "204":
          description: Link removed

  /stories/{id}/outputs:
    get:
      operationId: getStoryOutputs
      tags: [stories]
      summary: List Story outputs
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: number
      responses:
        "200":
          description: Story outputs
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/StoryOutput"
    post:
      operationId: createStoryOutput
      tags: [stories]
      summary: Create a Story output
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: number
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/StoryOutputInput"
      responses:
        "201":
          description: Created output
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/StoryOutput"

  /stories/{id}/health:
    get:
      operationId: getStoryHealth
      tags: [stories]
      summary: Calculate explained Story health
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: number
      responses:
        "200":
          description: Story health
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/StoryHealth"

  /stories/{id}/timeline:
    get:
      operationId: getStoryTimeline
      tags: [stories]
      summary: Get Story events and version checkpoints
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: number
      responses:
        "200":
          description: Story timeline
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/StoryEvent"

  /stories/{id}/archive:
    post:
      operationId: archiveStory
      tags: [stories]
      summary: Archive or restore a Story
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: number
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/StoryArchiveInput"
      responses:
        "200":
          description: Updated Story
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Story"

  # Campaigns
  /campaigns:
    get:
      operationId: listCampaigns
      tags: [campaigns]
      summary: List campaigns
      responses:
        "200":
          description: List of campaigns
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Campaign"
    post:
      operationId: createCampaign
      tags: [campaigns]
      summary: Create a campaign
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CampaignInput"
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Campaign"

  /campaigns/{id}:
    get:
      operationId: getCampaign
      tags: [campaigns]
      summary: Get a campaign
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: number
      responses:
        "200":
          description: Campaign
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Campaign"
        "404":
          description: Not found
    patch:
      operationId: updateCampaign
      tags: [campaigns]
      summary: Update a campaign
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: number
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CampaignPatch"
      responses:
        "200":
          description: Updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Campaign"
        "404":
          description: Not found
    delete:
      operationId: deleteCampaign
      tags: [campaigns]
      summary: Delete a campaign
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: number
      responses:
        "204":
          description: Deleted
        "404":
          description: Not found

  # Evidence Vault
  /evidence:
    get:
      operationId: listEvidence
      tags: [evidence]
      summary: List evidence items
      parameters:
        - name: type
          in: query
          schema:
            type: string
        - name: storyId
          in: query
          schema:
            type: ["number", "null"]
        - name: projectId
          in: query
          schema:
            type: ["number", "null"]
        - name: search
          in: query
          schema:
            type: string
      responses:
        "200":
          description: List of evidence
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Evidence"
    post:
      operationId: createEvidence
      tags: [evidence]
      summary: Create an evidence item
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/EvidenceInput"
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Evidence"

  /evidence/{id}:
    get:
      operationId: getEvidence
      tags: [evidence]
      summary: Get an evidence item
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: number
      responses:
        "200":
          description: Evidence item
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Evidence"
        "404":
          description: Not found
    patch:
      operationId: updateEvidence
      tags: [evidence]
      summary: Update an evidence item
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: number
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/EvidencePatch"
      responses:
        "200":
          description: Updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Evidence"
        "404":
          description: Not found
    delete:
      operationId: deleteEvidence
      tags: [evidence]
      summary: Delete an evidence item
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: number
      responses:
        "204":
          description: Deleted
        "404":
          description: Not found

  # Assets
  /assets:
    get:
      operationId: listAssets
      tags: [assets]
      summary: List assets
      parameters:
        - name: type
          in: query
          schema:
            type: string
        - name: storyId
          in: query
          schema:
            type: ["number", "null"]
      responses:
        "200":
          description: List of assets
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Asset"
    post:
      operationId: createAsset
      tags: [assets]
      summary: Create an asset
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetInput"
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Asset"

  /assets/{id}:
    get:
      operationId: getAsset
      tags: [assets]
      summary: Get an asset
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: number
      responses:
        "200":
          description: Asset
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Asset"
        "404":
          description: Not found
    patch:
      operationId: updateAsset
      tags: [assets]
      summary: Update an asset
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: number
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetPatch"
      responses:
        "200":
          description: Updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Asset"
        "404":
          description: Not found
    delete:
      operationId: deleteAsset
      tags: [assets]
      summary: Delete an asset
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: number
      responses:
        "204":
          description: Deleted
        "404":
          description: Not found

  # Knowledge Base
  /knowledge:
    get:
      operationId: listKnowledge
      tags: [knowledge]
      summary: List knowledge base pages
      parameters:
        - name: category
          in: query
          schema:
            type: string
        - name: search
          in: query
          schema:
            type: string
      responses:
        "200":
          description: List of knowledge pages
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/KnowledgePage"
    post:
      operationId: createKnowledge
      tags: [knowledge]
      summary: Create a knowledge page
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/KnowledgeInput"
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/KnowledgePage"

  /knowledge/{id}:
    get:
      operationId: getKnowledge
      tags: [knowledge]
      summary: Get a knowledge page
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: number
      responses:
        "200":
          description: Knowledge page
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/KnowledgePage"
        "404":
          description: Not found
    patch:
      operationId: updateKnowledge
      tags: [knowledge]
      summary: Update a knowledge page
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: number
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/KnowledgePatch"
      responses:
        "200":
          description: Updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/KnowledgePage"
        "404":
          description: Not found
    delete:
      operationId: deleteKnowledge
      tags: [knowledge]
      summary: Delete a knowledge page
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: number
      responses:
        "204":
          description: Deleted
        "404":
          description: Not found

  # Templates
  /templates:
    get:
      operationId: listTemplates
      tags: [templates]
      summary: List templates
      parameters:
        - name: type
          in: query
          schema:
            type: string
      responses:
        "200":
          description: List of templates
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Template"
    post:
      operationId: createTemplate
      tags: [templates]
      summary: Create a template
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/TemplateInput"
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Template"

  /templates/{id}:
    get:
      operationId: getTemplate
      tags: [templates]
      summary: Get a template
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: number
      responses:
        "200":
          description: Template
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Template"
        "404":
          description: Not found
    patch:
      operationId: updateTemplate
      tags: [templates]
      summary: Update a template
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: number
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/TemplatePatch"
      responses:
        "200":
          description: Updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Template"
        "404":
          description: Not found
    delete:
      operationId: deleteTemplate
      tags: [templates]
      summary: Delete a template
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: number
      responses:
        "204":
          description: Deleted
        "404":
          description: Not found

components:
  schemas:
    HealthStatus:
      type: object
      properties:
        status:
          type: string
      required:
        - status

    # Dashboard
    DashboardStats:
      type: object
      required: [totalStories, totalCampaigns, totalEvidence, totalAssets, totalKnowledge, storiesByStatus, activeCampaigns]
      properties:
        totalStories:
          type: number
        totalCampaigns:
          type: number
        totalEvidence:
          type: number
        totalAssets:
          type: number
        totalKnowledge:
          type: number
        activeCampaigns:
          type: number
        storiesByStatus:
          type: array
          items:
            $ref: "#/components/schemas/StatusCount"

    StatusCount:
      type: object
      required: [status, count]
      properties:
        status:
          type: string
        count:
          type: number

    ActivityItem:
      type: object
      required: [id, entityType, entityId, entityTitle, action, createdAt]
      properties:
        id:
          type: number
        entityType:
          type: string
        entityId:
          type: number
        entityTitle:
          type: string
        action:
          type: string
        createdAt:
          type: string
          format: date-time

    # Workspaces
    Workspace:
      type: object
      required: [id, name, slug, status, purpose, owner, tags, repositoryLinks, preferredExportFormats, knowledgeDomains, isFavorite, isPinned, createdAt, updatedAt]
      properties:
        id:
          type: number
        name:
          type: string
        slug:
          type: string
        description:
          type: ["string", "null"]
        color:
          type: ["string", "null"]
        status:
          type: string
          enum: [Active, Archived, Corrupted]
        purpose:
          type: string
          enum: [Product, Marketing, Research, Internal, Personal, Other]
        brand:
          type: ["string", "null"]
        writingVoice:
          type: ["string", "null"]
        targetAudience:
          type: ["string", "null"]
        currentGoal:
          type: ["string", "null"]
        icon:
          type: ["string", "null"]
        logoPath:
          type: ["string", "null"]
        owner:
          type: string
        tags:
          type: array
          items:
            type: string
        repositoryLinks:
          type: array
          items:
            type: string
        preferredExportFormats:
          type: array
          items:
            type: string
        knowledgeDomains:
          type: array
          items:
            type: string
        isFavorite:
          type: boolean
        isPinned:
          type: boolean
        lastOpenedAt:
          type: ["string", "null"]
          format: date-time
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    WorkspaceInput:
      type: object
      required: [name]
      properties:
        name:
          type: string
          minLength: 1
        description:
          type: string
        color:
          type: string
        purpose:
          type: string
          enum: [Product, Marketing, Research, Internal, Personal, Other]
        brand:
          type: string
        writingVoice:
          type: string
        targetAudience:
          type: string
        currentGoal:
          type: string
        tags:
          type: array
          items:
            type: string
        repositoryLinks:
          type: array
          items:
            type: string
        preferredExportFormats:
          type: array
          items:
            type: string
        knowledgeDomains:
          type: array
          items:
            type: string

    WorkspacePatch:
      type: object
      properties:
        name:
          type: string
          minLength: 1
        description:
          type: ["string", "null"]
        color:
          type: ["string", "null"]
        status:
          type: string
          enum: [Active, Archived, Corrupted]
        purpose:
          type: string
          enum: [Product, Marketing, Research, Internal, Personal, Other]
        brand:
          type: ["string", "null"]
        writingVoice:
          type: ["string", "null"]
        targetAudience:
          type: ["string", "null"]
        currentGoal:
          type: ["string", "null"]
        tags:
          type: array
          items:
            type: string
        repositoryLinks:
          type: array
          items:
            type: string
        preferredExportFormats:
          type: array
          items:
            type: string
        knowledgeDomains:
          type: array
          items:
            type: string
        isFavorite:
          type: boolean
        isPinned:
          type: boolean

    WorkspaceMetrics:
      type: object
      required: [stories, evidence, knowledge, campaigns, assets, exports]
      properties:
        stories:
          type: number
        evidence:
          type: number
        knowledge:
          type: number
        campaigns:
          type: number
        assets:
          type: number
        exports:
          type: number

    WorkspaceHealthComponent:
      type: object
      required: [key, label, score, applicable, explanation]
      properties:
        key:
          type: string
        label:
          type: string
        score:
          type: number
        applicable:
          type: boolean
        explanation:
          type: string

    WorkspaceHealth:
      type: object
      required: [score, insufficientData, components]
      properties:
        score:
          type: number
        insufficientData:
          type: boolean
        components:
          type: array
          items:
            $ref: "#/components/schemas/WorkspaceHealthComponent"

    WorkspaceSummary:
      allOf:
        - $ref: "#/components/schemas/Workspace"
        - type: object
          required: [metrics, health]
          properties:
            metrics:
              $ref: "#/components/schemas/WorkspaceMetrics"
            health:
              $ref: "#/components/schemas/WorkspaceHealth"

    WorkspaceOverview:
      allOf:
        - $ref: "#/components/schemas/WorkspaceSummary"
        - type: object
          required: [recentActivity]
          properties:
            recentActivity:
              type: array
              items:
                $ref: "#/components/schemas/ActivityItem"
            continueWorking:
              anyOf:
                - type: object
                  required: [id, title, status]
                  properties:
                    id:
                      type: number
                    title:
                      type: string
                    status:
                      type: string
                - type: "null"

    WorkspaceIntegrity:
      type: object
      required: [healthy, issues]
      properties:
        healthy:
          type: boolean
        issues:
          type: array
          items:
            type: string

    WorkspaceManifest:
      type: object
      required: [schemaVersion, exportedAt, workspace, metrics]
      properties:
        schemaVersion:
          type: number
        exportedAt:
          type: string
          format: date-time
        workspace:
          $ref: "#/components/schemas/Workspace"
        metrics:
          $ref: "#/components/schemas/WorkspaceMetrics"

    # Deprecated Projects
    Project:
      type: object
      required: [id, name, createdAt, updatedAt]
      properties:
        id:
          type: number
        name:
          type: string
        description:
          type: ["string", "null"]
        color:
          type: ["string", "null"]
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    ProjectInput:
      type: object
      required: [name]
      properties:
        name:
          type: string
          minLength: 1
        description:
          type: string
        color:
          type: string

    ProjectPatch:
      type: object
      properties:
        name:
          type: string
          minLength: 1
        description:
          type: ["string", "null"]
        color:
          type: ["string", "null"]

    # Stories
    Story:
      type: object
      required: [id, title, status, createdAt, updatedAt]
      properties:
        id:
          type: number
        title:
          type: string
        slug:
          type: ["string", "null"]
        summary:
          type: ["string", "null"]
        content:
          type: ["string", "null"]
        status:
          type: string
          enum: [Idea, Research, EvidenceGathering, Outline, Draft, Review, Approved, Published, Archived]
        category:
          type: ["string", "null"]
        priority:
          type: ["string", "null"]
          enum: [Low, Medium, High, Critical, null]
        audience:
          type: ["string", "null"]
        difficulty:
          type: ["string", "null"]
          enum: [Beginner, Intermediate, Advanced, null]
        tags:
          type: array
          items:
            type: string
        projectId:
          type: ["number", "null"]
        workspaceId:
          type: ["number", "null"]
        campaignId:
          type: ["number", "null"]
        evidenceScore:
          type: ["number", "null"]
        storyType:
          type: string
          enum: [EngineeringJournal, BlogArticle, SocialSeries, CaseStudy, TechnicalDocumentation, ADR, ResearchNote, LearningNote, ProductUpdate, ChangelogNarrative, InternalMemo, Presentation, Other]
        author:
          type: ["string", "null"]
        objective:
          type: ["string", "null"]
        targetPlatforms:
          type: array
          items:
            type: string
        publishAt:
          type: ["string", "null"]
          format: date-time
        wordCount:
          type: number
        estimatedReadMinutes:
          type: number
        version:
          type: number
        archivedAt:
          type: ["string", "null"]
          format: date-time
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    StoryInput:
      type: object
      required: [title, workspaceId]
      properties:
        title:
          type: string
          minLength: 1
        summary:
          type: string
        content:
          type: string
        status:
          type: string
          enum: [Idea, Research, EvidenceGathering, Outline, Draft, Review, Approved, Published, Archived]
        category:
          type: string
        priority:
          type: string
          enum: [Low, Medium, High, Critical]
        audience:
          type: string
        difficulty:
          type: string
          enum: [Beginner, Intermediate, Advanced]
        tags:
          type: array
          items:
            type: string
        projectId:
          type: ["number", "null"]
        workspaceId:
          type: number
        campaignId:
          type: ["number", "null"]
        storyType:
          type: string
          enum: [EngineeringJournal, BlogArticle, SocialSeries, CaseStudy, TechnicalDocumentation, ADR, ResearchNote, LearningNote, ProductUpdate, ChangelogNarrative, InternalMemo, Presentation, Other]
        author:
          type: string
        objective:
          type: string
        targetPlatforms:
          type: array
          items:
            type: string
        publishAt:
          type: string
          format: date-time

    StoryPatch:
      type: object
      properties:
        title:
          type: string
          minLength: 1
        summary:
          type: ["string", "null"]
        content:
          type: ["string", "null"]
        status:
          type: string
          enum: [Idea, Research, EvidenceGathering, Outline, Draft, Review, Approved, Published, Archived]
        category:
          type: ["string", "null"]
        priority:
          type: ["string", "null"]
          enum: [Low, Medium, High, Critical, null]
        audience:
          type: ["string", "null"]
        difficulty:
          type: ["string", "null"]
          enum: [Beginner, Intermediate, Advanced, null]
        tags:
          type: array
          items:
            type: string
        projectId:
          type: ["number", "null"]
        workspaceId:
          type: ["number", "null"]
        campaignId:
          type: ["number", "null"]
        storyType:
          type: string
          enum: [EngineeringJournal, BlogArticle, SocialSeries, CaseStudy, TechnicalDocumentation, ADR, ResearchNote, LearningNote, ProductUpdate, ChangelogNarrative, InternalMemo, Presentation, Other]
        author:
          type: ["string", "null"]
        objective:
          type: ["string", "null"]
        targetPlatforms:
          type: array
          items:
            type: string
        publishAt:
          type: ["string", "null"]
          format: date-time
        expectedVersion:
          type: number

    StoryTransitionInput:
      type: object
      required: [status]
      properties:
        status:
          type: string
          enum: [Idea, Research, EvidenceGathering, Outline, Draft, Review, Approved, Published, Archived]

    StoryArchiveInput:
      type: object
      required: [archived]
      properties:
        archived:
          type: boolean

    StoryOutlineItem:
      type: object
      required: [id, storyId, position, title, completionStatus, createdAt, updatedAt]
      properties:
        id: { type: number }
        storyId: { type: number }
        parentId: { type: ["number", "null"] }
        position: { type: number }
        title: { type: string }
        notes: { type: ["string", "null"] }
        completionStatus:
          type: string
          enum: [Planned, InProgress, Complete]
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }

    StoryOutlineItemInput:
      type: object
      required: [title, completionStatus]
      properties:
        id: { type: number }
        parentId: { type: ["number", "null"] }
        title: { type: string, minLength: 1 }
        notes: { type: ["string", "null"] }
        completionStatus:
          type: string
          enum: [Planned, InProgress, Complete]

    StoryLinkInput:
      type: object
      required: [entityType, entityId]
      properties:
        entityType:
          type: string
          enum: [evidence, knowledge, asset, campaign, story]
        entityId: { type: number }
        relationshipType: { type: string }
        notes: { type: string }

    StoryLinkedEntity:
      type: object
      required: [id, title, entityType]
      properties:
        id: { type: number }
        title: { type: string }
        entityType: { type: string }
        relationshipType: { type: ["string", "null"] }
        notes: { type: ["string", "null"] }

    StoryLinks:
      type: object
      required: [evidence, knowledge, assets, campaigns, stories]
      properties:
        evidence: { type: array, items: { $ref: "#/components/schemas/StoryLinkedEntity" } }
        knowledge: { type: array, items: { $ref: "#/components/schemas/StoryLinkedEntity" } }
        assets: { type: array, items: { $ref: "#/components/schemas/StoryLinkedEntity" } }
        campaigns: { type: array, items: { $ref: "#/components/schemas/StoryLinkedEntity" } }
        stories: { type: array, items: { $ref: "#/components/schemas/StoryLinkedEntity" } }

    StoryOutput:
      type: object
      required: [id, storyId, type, title, status, createdAt, updatedAt]
      properties:
        id: { type: number }
        storyId: { type: number }
        type: { type: string }
        title: { type: string }
        status:
          type: string
          enum: [Draft, Review, Ready, Published, Archived]
        content: { type: ["string", "null"] }
        format: { type: ["string", "null"] }
        destination: { type: ["string", "null"] }
        publishedAt: { type: ["string", "null"], format: date-time }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }

    StoryOutputInput:
      type: object
      required: [type, title]
      properties:
        type: { type: string }
        title: { type: string, minLength: 1 }
        content: { type: string }
        format: { type: string }
        destination: { type: string }

    StoryHealthComponent:
      type: object
      required: [key, label, score, applicable, explanation]
      properties:
        key: { type: string }
        label: { type: string }
        score: { type: number }
        applicable: { type: boolean }
        explanation: { type: string }

    StoryHealth:
      type: object
      required: [score, insufficientData, blockers, components]
      properties:
        score: { type: number }
        insufficientData: { type: boolean }
        blockers: { type: array, items: { type: string } }
        components:
          type: array
          items:
            $ref: "#/components/schemas/StoryHealthComponent"

    StoryEvent:
      type: object
      required: [id, eventType, actor, payload, createdAt]
      properties:
        id: { type: number }
        eventType: { type: string }
        actor: { type: string }
        payload: { type: object, additionalProperties: true }
        createdAt: { type: string, format: date-time }

    # Campaigns
    Campaign:
      type: object
      required: [id, title, status, createdAt, updatedAt]
      properties:
        id:
          type: number
        title:
          type: string
        objective:
          type: ["string", "null"]
        status:
          type: string
          enum: [Planning, Active, Paused, Completed, Archived]
        platforms:
          type: array
          items:
            type: string
        durationWeeks:
          type: ["number", "null"]
        storyCount:
          type: number
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    CampaignInput:
      type: object
      required: [title]
      properties:
        title:
          type: string
          minLength: 1
        objective:
          type: string
        status:
          type: string
          enum: [Planning, Active, Paused, Completed, Archived]
        platforms:
          type: array
          items:
            type: string
        durationWeeks:
          type: number

    CampaignPatch:
      type: object
      properties:
        title:
          type: string
          minLength: 1
        objective:
          type: ["string", "null"]
        status:
          type: string
          enum: [Planning, Active, Paused, Completed, Archived]
        platforms:
          type: array
          items:
            type: string
        durationWeeks:
          type: ["number", "null"]

    # Evidence
    Evidence:
      type: object
      required: [id, title, type, createdAt, updatedAt]
      properties:
        id:
          type: number
        title:
          type: string
        type:
          type: string
          enum: [Screenshot, TerminalOutput, GitLog, Benchmark, Diagram, MeetingNotes, ResearchPDF, Image, Video, VoiceRecording, CodeSnippet, IssueReport, BuildLog, RepositoryAudit, Other]
        source:
          type: ["string", "null"]
        notes:
          type: ["string", "null"]
        content:
          type: ["string", "null"]
        tags:
          type: array
          items:
            type: string
        storyId:
          type: ["number", "null"]
        projectId:
          type: ["number", "null"]
        repository:
          type: ["string", "null"]
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    EvidenceInput:
      type: object
      required: [title, type]
      properties:
        title:
          type: string
          minLength: 1
        type:
          type: string
          enum: [Screenshot, TerminalOutput, GitLog, Benchmark, Diagram, MeetingNotes, ResearchPDF, Image, Video, VoiceRecording, CodeSnippet, IssueReport, BuildLog, RepositoryAudit, Other]
        source:
          type: string
        notes:
          type: string
        content:
          type: string
        tags:
          type: array
          items:
            type: string
        storyId:
          type: ["number", "null"]
        projectId:
          type: ["number", "null"]
        repository:
          type: string

    EvidencePatch:
      type: object
      properties:
        title:
          type: string
          minLength: 1
        type:
          type: string
          enum: [Screenshot, TerminalOutput, GitLog, Benchmark, Diagram, MeetingNotes, ResearchPDF, Image, Video, VoiceRecording, CodeSnippet, IssueReport, BuildLog, RepositoryAudit, Other]
        source:
          type: ["string", "null"]
        notes:
          type: ["string", "null"]
        content:
          type: ["string", "null"]
        tags:
          type: array
          items:
            type: string
        storyId:
          type: ["number", "null"]
        projectId:
          type: ["number", "null"]
        repository:
          type: ["string", "null"]

    # Assets
    Asset:
      type: object
      required: [id, title, type, createdAt, updatedAt]
      properties:
        id:
          type: number
        title:
          type: string
        type:
          type: string
          enum: [Image, Icon, Illustration, Logo, Video, SVG, PDF, BrandAsset, ScreenRecording, Other]
        url:
          type: ["string", "null"]
        filePath:
          type: ["string", "null"]
        tags:
          type: array
          items:
            type: string
        storyId:
          type: ["number", "null"]
        campaignId:
          type: ["number", "null"]
        projectId:
          type: ["number", "null"]
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    AssetInput:
      type: object
      required: [title, type]
      properties:
        title:
          type: string
          minLength: 1
        type:
          type: string
          enum: [Image, Icon, Illustration, Logo, Video, SVG, PDF, BrandAsset, ScreenRecording, Other]
        url:
          type: string
        filePath:
          type: string
        tags:
          type: array
          items:
            type: string
        storyId:
          type: ["number", "null"]
        campaignId:
          type: ["number", "null"]
        projectId:
          type: ["number", "null"]

    AssetPatch:
      type: object
      properties:
        title:
          type: string
          minLength: 1
        type:
          type: string
          enum: [Image, Icon, Illustration, Logo, Video, SVG, PDF, BrandAsset, ScreenRecording, Other]
        url:
          type: ["string", "null"]
        filePath:
          type: ["string", "null"]
        tags:
          type: array
          items:
            type: string
        storyId:
          type: ["number", "null"]
        campaignId:
          type: ["number", "null"]
        projectId:
          type: ["number", "null"]

    # Knowledge Base
    KnowledgePage:
      type: object
      required: [id, title, createdAt, updatedAt]
      properties:
        id:
          type: number
        title:
          type: string
        content:
          type: ["string", "null"]
        category:
          type: ["string", "null"]
        tags:
          type: array
          items:
            type: string
        linkedPageIds:
          type: array
          items:
            type: number
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    KnowledgeInput:
      type: object
      required: [title]
      properties:
        title:
          type: string
          minLength: 1
        content:
          type: string
        category:
          type: string
        tags:
          type: array
          items:
            type: string
        linkedPageIds:
          type: array
          items:
            type: number

    KnowledgePatch:
      type: object
      properties:
        title:
          type: string
          minLength: 1
        content:
          type: ["string", "null"]
        category:
          type: ["string", "null"]
        tags:
          type: array
          items:
            type: string
        linkedPageIds:
          type: array
          items:
            type: number

    # Templates
    Template:
      type: object
      required: [id, title, type, createdAt, updatedAt]
      properties:
        id:
          type: number
        title:
          type: string
        type:
          type: string
          enum: [LinkedIn, Blog, XThread, Facebook, ReleaseNotes, CaseStudy, EngineeringJournal, RepositoryAudit, ArchitectureReview, SprintReview, Retrospective, Other]
        content:
          type: ["string", "null"]
        description:
          type: ["string", "null"]
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    TemplateInput:
      type: object
      required: [title, type]
      properties:
        title:
          type: string
          minLength: 1
        type:
          type: string
          enum: [LinkedIn, Blog, XThread, Facebook, ReleaseNotes, CaseStudy, EngineeringJournal, RepositoryAudit, ArchitectureReview, SprintReview, Retrospective, Other]
        content:
          type: string
        description:
          type: string

    TemplatePatch:
      type: object
      properties:
        title:
          type: string
          minLength: 1
        type:
          type: string
          enum: [LinkedIn, Blog, XThread, Facebook, ReleaseNotes, CaseStudy, EngineeringJournal, RepositoryAudit, ArchitectureReview, SprintReview, Retrospective, Other]
        content:
          type: ["string", "null"]
        description:
          type: ["string", "null"]

    SearchResult:
      type: object
      required: [entityType, entityId, title, snippet, path]
      properties:
        entityType:
          type: string
          enum: [story, evidence, knowledge, campaign, asset, template, project]
        entityId:
          type: number
        title:
          type: string
        snippet:
          type: string
        path:
          type: string
